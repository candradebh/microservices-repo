spring: 
  datasource:
    driverClassName: org.postgresql.Driver
    password: sa
    username: postgres
    url: jdbc:postgresql://localhost:5432/ca_auth
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect

     
